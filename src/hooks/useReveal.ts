/**
 * V1 scroll-reveal hook shim — no-op in V2.
 * All scroll animations now handled by Framer Motion in individual components.
 * Preserved pages import this without modification.
 */
export default function useReveal() {}
